# Othello (single-player)

Scaffolded Vite + React + TypeScript app with a simple Othello engine and AI (random, greedy, minimax).

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173
